# ========================================
# HG 585 LLM Evaluation System - Requirements
# Choose the appropriate requirements file for your needs
# ========================================

# OPTION 1: Minimal Installation (Recommended for testing)
# pip install -r requirements-minimal.txt
# Size: ~2-3GB, Basic functionality only

# OPTION 2: Full Installation (Recommended for production)
# pip install -r requirements-full.txt  
# Size: ~8-10GB, Complete functionality

# OPTION 3: Chat Interface Only (For using trained models)
# pip install -r requirements-chat.txt
# Size: ~3-4GB, For deployment of trained models

# ========================================
# Quick Start (Minimal):
# ========================================
torch>=2.1.0
transformers>=4.35.0
accelerate>=0.24.0
peft>=0.6.0
huggingface-hub[cli]>=0.19.0
tokenizers>=0.15.0
python-docx>=1.2.0
PyPDF2>=3.0.1
pandas>=2.0.0
numpy>=1.24.0
psutil>=5.9.0
tqdm>=4.66.0
requests>=2.31.0

# ========================================
# For GPU support, also run:
# pip install torch --index-url https://download.pytorch.org/whl/cu118
# ========================================
